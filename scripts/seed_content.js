#!/usr/bin/env node
// Fills lesson content. Idempotent — safe to re-run.
const { Pool } = require('pg')

const pool = new Pool({ connectionString: process.env.DATABASE_URL })

const lessons = [
  {
    slug: 'context-claude-code-necessary-intro',
    content: `## Контекст, который сделал Claude Code необходимым

Чтобы понять, зачем появился Claude Code, нужно посмотреть на то, как менялась разработка ПО за последние годы.

### Эволюция инструментов разработчика

Программирование всегда требовало управления огромной сложностью: понимать предметную область, удерживать в голове архитектуру, писать код, тестировать и исправлять ошибки. Инструменты — IDE, линтеры, системы сборки — снижали когнитивную нагрузку, но суть оставалась прежней: разработчик пишет, машина выполняет.

С появлением больших языковых моделей возникло принципиально новое взаимодействие: теперь машина может **понимать намерение** и генерировать код. Первые эксперименты (GitHub Copilot, ChatGPT) показали огромный потенциал, но и ограничение: ИИ давал ответы в диалоге, а разработчик всё равно вручную копировал код, запускал тесты, устранял ошибки.

### Разрыв между чатом и терминалом

Проблема была в разрыве: ИИ-ассистент жил в браузере, а работа — в терминале и редакторе. Каждый шаг требовал переключения контекста:

1. Описать задачу в чате
2. Получить код
3. Скопировать в файл
4. Запустить
5. Увидеть ошибку
6. Вернуться в чат, описать ошибку
7. Повторить

Это «копирование туда-обратно» съедало то время, которое ИИ должен был экономить.

### Почему нужен агент, а не чат

Настоящая производительность приходит, когда ИИ может работать **автономно**: читать файлы, редактировать код, выполнять команды, видеть результат и исправлять ошибки — в одном непрерывном цикле. Именно это и есть Claude Code.

### Итог

- Чат-ассистенты полезны, но требуют ручного переноса работы
- Разрыв между диалогом и терминалом снижает продуктивность
- Агентный подход устраняет этот разрыв: ИИ работает там, где живёт код
`
  },
  {
    slug: 'anthropic-background-intro',
    content: `## Anthropic — бэкграунд и назначение

Anthropic — это компания по безопасности ИИ, создавшая Claude. Понимание её миссии объясняет, почему Claude Code устроен именно так.

### История компании

Anthropic основана в 2021 году бывшими сотрудниками OpenAI — в том числе Дарио Амодеем (CEO) и Дэниелой Амодеей (President). Команда ушла с целью сосредоточиться на безопасности ИИ как первостепенной задаче, а не как дополнении к продукту.

Ключевые вехи:
- **2021** — основание компании, начало исследований Constitutional AI
- **2023** — релиз Claude 1, затем Claude 2
- **2024** — Claude 3 (Haiku, Sonnet, Opus), признание лучшей моделью по многим бенчмаркам
- **2025** — Claude Code как полноценный агент для разработчиков

### Constitutional AI

Anthropic разработала подход Constitutional AI (CAI) — метод обучения, при котором модель следует явно заданным принципам. Это отличается от простого RLHF (обучение с обратной связью от людей): модель учится рассуждать о том, почему тот или иной ответ лучше, а не только имитировать предпочтения разметчиков.

Для разработчиков это означает: Claude склонен объяснять своё мышление, признавать неопределённость и отказываться от потенциально вредных действий даже без явного запрета.

### Позиция Anthropic в индустрии

Anthropic позиционирует себя как «safety-first» компанию, но при этом активно участвует в гонке за лучшие модели. Логика такова: если мощные ИИ-системы будут созданы в любом случае, лучше, чтобы их создавали те, кто ставит безопасность во главу угла.

### Почему это важно для Claude Code

- Claude Code создан компанией, для которой предсказуемость и безопасность — не маркетинг, а исследовательский приоритет
- Принцип «спрашивай перед деструктивными действиями» встроен в модель на уровне обучения
- Прозрачность в объяснении шагов — следствие Constitutional AI

### Итог

- Anthropic — компания по безопасности ИИ, не просто AI-стартап
- Constitutional AI обеспечивает предсказуемое и объяснимое поведение Claude
- Это напрямую влияет на то, как Claude Code принимает решения в вашем проекте
`
  },
  {
    slug: 'claude-model-family-intro',
    content: `## Семейство моделей Claude

Claude Code работает на моделях Claude. Понимание семейства помогает выбрать правильный баланс между скоростью и качеством.

### Три уровня моделей

Anthropic выпускает модели в трёх уровнях:

| Уровень | Модель | Характеристика |
|---------|--------|---------------|
| Быстрый | Claude Haiku | Наименьшие задержки, ниже стоимость, хорош для простых задач |
| Сбалансированный | Claude Sonnet | Оптимальный баланс скорости и интеллекта |
| Мощный | Claude Opus | Максимальные возможности, для сложных задач |

### Актуальные версии (2025)

- **claude-haiku-4-5** — быстрый, для рутинных задач
- **claude-sonnet-4-6** — рекомендуемый для большинства задач разработки
- **claude-opus-4-7** — для архитектурных решений, сложного анализа

### Как Claude Code выбирает модель

По умолчанию Claude Code использует последний Sonnet. Это намеренное решение: Sonnet достаточно умён для большинства задач разработки и работает существенно быстрее Opus.

Переключить модель можно через настройку:
\`\`\`bash
claude config set model claude-opus-4-7
\`\`\`

Или через флаг для одного вызова:
\`\`\`bash
claude --model claude-opus-4-7 "Проанализируй архитектуру этого проекта"
\`\`\`

### Fast Mode

В Claude Code есть режим Fast Mode (переключается через \`/fast\`). Он не меняет модель на меньшую, а оптимизирует вывод для скорости при той же модели.

### Контекстное окно

Разные модели поддерживают разный размер контекста. Для Claude Code важно, что модели семейства Claude 4 поддерживают **200K токенов** — это позволяет загружать в контекст целые кодовые базы.

### Итог

- Haiku → скорость, Sonnet → баланс, Opus → мощь
- Claude Code по умолчанию использует Sonnet — оптимальный выбор для разработки
- Модель можно переключить в настройках или флагом \`--model\`
`
  },
  {
    slug: 'what-is-claude-code-intro',
    content: `## Что такое Claude Code

Claude Code — это агентный ИИ-инструмент для разработчиков, работающий прямо в терминале. В отличие от чат-ассистентов, он не просто отвечает на вопросы — он действует.

### Ключевые характеристики

**Агент, а не чат-бот.** Claude Code может:
- Читать и редактировать файлы проекта
- Выполнять команды в терминале
- Запускать тесты и анализировать их вывод
- Искать информацию в интернете
- Взаимодействовать с внешними сервисами через MCP

**Работает там, где живёт код.** Запуск прямо в папке проекта — Claude Code видит контекст: структуру файлов, git-историю, package.json, конфиги.

**Итеративный цикл.** Когда задача сложная, Claude Code работает в цикле: выполняет действие → видит результат → принимает следующее решение. Это называется агентным циклом.

### Как выглядит типичная сессия

\`\`\`
$ cd my-project
$ claude

> Добавь эндпоинт GET /api/users, который возвращает список пользователей из PostgreSQL

● Читаю структуру проекта...
● Нашёл: app/api/, lib/db.ts, types/index.ts
● Создаю app/api/users/route.ts
● Добавляю тип User в types/index.ts
● Готово. Эндпоинт создан, использует существующий пул соединений из lib/db.ts
\`\`\`

### Что Claude Code умеет

- **Понимать контекст проекта** — читает файлы, структуру, зависимости
- **Писать и редактировать код** — создаёт новые файлы, изменяет существующие
- **Выполнять команды** — npm test, git commit, docker build
- **Исправлять ошибки** — видит вывод команды и самостоятельно устраняет проблему
- **Объяснять код** — рассказывает, как работает любая часть кодовой базы

### Чем Claude Code отличается от Copilot

| | GitHub Copilot | Claude Code |
|---|---|---|
| Где работает | В редакторе, строчные подсказки | В терминале, полные задачи |
| Что делает | Автодополнение кода | Выполняет задачи end-to-end |
| Видит контекст | Открытый файл | Весь проект |
| Может запускать | Нет | Да, команды терминала |

### Итог

- Claude Code — агент, а не автодополнение
- Работает в терминале, видит весь проект
- Выполняет задачи в итеративном цикле: действие → результат → следующее действие
`
  },
  {
    slug: 'why-developers-needed-it-intro',
    content: `## Почему сообществу разработчиков это было необходимо

Появление Claude Code — ответ на конкретные боли, которые испытывали разработчики при работе с ИИ-инструментами первого поколения.

### Боль №1: Потеря контекста между чатом и кодом

При работе с ChatGPT или Claude в браузере разработчик постоянно копирует:
- Код из файла → в чат (чтобы описать задачу)
- Ответ из чата → в файл
- Ошибку из терминала → в чат
- Исправление → обратно в файл

Это не просто неудобно — это разрывает поток мышления. Каждое переключение контекста стоит когнитивных усилий.

### Боль №2: ИИ не видит «всю картину»

Когда разработчик описывает задачу в чате, он вынужден сам собирать контекст: копировать нужные файлы, объяснять структуру проекта, описывать зависимости. ИИ работает с неполной информацией и часто предлагает решения, несовместимые с реальным проектом.

### Боль №3: Разрыв «сказал — сделал»

ИИ мог написать правильный код, но разработчик всё равно должен был:
- Применить изменения вручную
- Запустить тесты
- Разобраться с ошибками сборки
- Снова пойти в чат

ИИ был советником, а не исполнителем.

### Что изменил Claude Code

| Старая схема | Claude Code |
|---|---|
| Копировать код в чат | Читает файлы сам |
| Объяснять структуру проекта | Исследует проект самостоятельно |
| Применять изменения вручную | Редактирует файлы напрямую |
| Копировать ошибки в чат | Видит вывод команд, исправляет сам |

### Реальный эффект

По данным Anthropic и отчётам разработчиков, Claude Code позволяет:
- Реализовывать фичи за часы вместо дней
- Разбираться в незнакомых кодовых базах в разы быстрее
- Заниматься рефакторингом без страха что-то сломать
- Автоматизировать рутинные задачи (миграции, конфиги, документация)

### Итог

- Разработчики теряли продуктивность на переключении между чатом и терминалом
- ИИ без контекста проекта давал неточные советы
- Claude Code решает это: видит проект целиком и действует, а не только отвечает
`
  },
  {
    slug: 'installation-setup-intro',
    content: `## Установка и первоначальная настройка

Claude Code устанавливается как глобальный npm-пакет и требует API-ключ или подписку Anthropic.

### Требования

- Node.js 18 или новее
- npm / npx
- macOS, Linux, или Windows (через WSL2)

### Установка

\`\`\`bash
npm install -g @anthropic-ai/claude-code
\`\`\`

После установки доступна команда \`claude\`.

### Аутентификация

Есть два способа:

**1. Через подписку Claude.ai (рекомендуется для начала)**
\`\`\`bash
claude
# Откроется браузер для входа через claude.ai
\`\`\`

**2. Через API-ключ (для продвинутого использования)**
\`\`\`bash
export ANTHROPIC_API_KEY=sk-ant-...
claude
\`\`\`

Ключ можно получить в [console.anthropic.com](https://console.anthropic.com).

### Первый запуск

\`\`\`bash
cd your-project
claude
\`\`\`

При первом запуске Claude Code:
1. Проверяет аутентификацию
2. Сканирует проект (ищет CLAUDE.md, package.json, git)
3. Готов принимать задачи

### Проверка установки

\`\`\`bash
claude --version
# claude-code/1.x.x

claude --help
# Список всех флагов и опций
\`\`\`

### Обновление

\`\`\`bash
npm update -g @anthropic-ai/claude-code
\`\`\`

### Удаление

\`\`\`bash
npm uninstall -g @anthropic-ai/claude-code
\`\`\`

### Настройки

Базовые настройки хранятся в \`~/.claude/settings.json\`. Просмотр и изменение через CLI:

\`\`\`bash
claude config list          # Показать все настройки
claude config set key value # Изменить настройку
\`\`\`

### Итог

- Установка: \`npm install -g @anthropic-ai/claude-code\`
- Аутентификация: подписка claude.ai или API-ключ
- Первый запуск: \`cd project && claude\`
- Обновление: \`npm update -g @anthropic-ai/claude-code\`
`
  },
  {
    slug: 'vscode-extension-intro',
    content: `## VS Code и расширение Claude Code

Claude Code интегрируется с VS Code через официальное расширение, обеспечивая работу прямо в редакторе.

### Установка расширения

1. Открыть VS Code
2. Cmd/Ctrl + Shift + X (Extensions)
3. Найти «Claude Code»
4. Установить официальное расширение от Anthropic

Или через CLI:
\`\`\`bash
code --install-extension anthropic.claude-code
\`\`\`

### Возможности расширения

**Встроенный терминал с Claude Code.** Расширение открывает панель Claude Code прямо в VS Code — не нужно переключаться в отдельный терминал.

**Контекст из редактора.** Claude Code видит:
- Открытый файл и позицию курсора
- Выделенный фрагмент кода
- Структуру workspace

**Inline diff.** Когда Claude Code вносит изменения, VS Code показывает diff прямо в редакторе — можно принять или отклонить каждое изменение.

### Keyboard shortcuts

| Shortcut | Действие |
|----------|---------|
| Cmd/Ctrl + Esc | Открыть/закрыть панель Claude Code |
| Cmd/Ctrl + Shift + E | Отправить выделенный код в Claude |

### Работа с выделением

Выделите фрагмент кода → нажмите правой кнопкой → «Ask Claude» — и Claude Code получит этот код как контекст.

\`\`\`
// Выделите функцию и спросите:
"Объясни что делает эта функция и где может упасть"
\`\`\`

### IDE integrations (JetBrains)

Помимо VS Code, расширение доступно для JetBrains IDE (IntelliJ, WebStorm, PyCharm):
- JetBrains Marketplace → Claude Code

### Итог

- Расширение устраняет переключение между VS Code и терминалом
- Показывает inline diff для каждого изменения
- Поддерживает контекст из выделенного кода
- Доступно для VS Code и JetBrains IDE
`
  },
  {
    slug: 'subscriptions-tokens-cost-intro',
    content: `## Подписки, стоимость токенов и использование

Понимание стоимости Claude Code помогает избежать неожиданных счётов и оптимизировать использование.

### Способы доступа

**1. Claude.ai Pro/Max — наиболее доступный вариант**

Подписка включает использование Claude Code с ограниченным количеством сообщений в час. При достижении лимита — ожидание сброса.

- Pro: $20/мес, базовый лимит
- Max: $100/мес, 5× больший лимит и доступ к Opus

**2. API через Anthropic Console — для команд и тяжёлого использования**

Оплата по токенам. Подходит, если нужен точный контроль расходов.

### Стоимость токенов (2025)

| Модель | Input (за 1M) | Output (за 1M) |
|--------|--------------|----------------|
| Haiku 4.5 | $0.80 | $4.00 |
| Sonnet 4.6 | $3.00 | $15.00 |
| Opus 4.7 | $15.00 | $75.00 |

### Что потребляет токены

Каждый запрос к Claude Code включает:
- **Контекст** — файлы, которые Claude читал в этой сессии
- **История диалога** — предыдущие сообщения сессии
- **Системный промпт** — внутренние инструкции Claude Code
- **Ваш запрос** — то, что вы написали
- **Ответ** — сгенерированный код/текст

Большие файлы и длинные сессии быстро накапливают токены.

### Оптимизация расходов

\`\`\`bash
# Начать новую сессию (сбросить контекст)
/clear

# Использовать Haiku для простых задач
claude --model claude-haiku-4-5 "Переименуй переменную x в userId"

# Проверить использование
claude config get
\`\`\`

### Просмотр статистики

Claude Code показывает количество использованных токенов в конце каждой сессии. При работе через API Console — детальный дашборд расходов.

### Итог

- Подписка Claude.ai — проще, ограничена по лимиту
- API — точный контроль, оплата по потреблению
- Sonnet — оптимальный баланс стоимости и качества для разработки
- Длинные сессии и большие файлы = больше токенов
`
  },
  {
    slug: 'first-session-intro',
    content: `## Работа в первой сессии

Первая сессия с Claude Code задаёт паттерн работы. Важно с самого начала правильно давать задачи и понимать, что происходит.

### Запуск

\`\`\`bash
cd your-project
claude
\`\`\`

Claude Code поприветствует вас и будет ждать задачу. Нет нужды в «магических» формулировках — описывайте задачу естественным языком.

### Первые задачи: с чего начать

Хорошие задачи для начала:
\`\`\`
> Изучи структуру этого проекта и объясни как он устроен
> Что делает функция processPayment в src/billing.ts?
> Найди все места, где используется устаревший API v1
\`\`\`

### Как Claude Code работает с файлами

Claude Code не загружает весь проект в контекст сразу. Он:
1. Читает структуру директорий
2. Открывает файлы по мере необходимости
3. Держит в контексте только то, что релевантно задаче

Вы увидите что-то вроде:
\`\`\`
● Reading src/billing.ts...
● Reading src/types/payment.ts...
● Analyzing...
\`\`\`

### Разрешения на действия

При первом действии, которое изменяет файлы или выполняет команды, Claude Code запросит разрешение:
\`\`\`
● Хочу создать файл src/api/users.ts. Разрешить? [y/n]
\`\`\`

Режимы разрешений:
- **По умолчанию** — спрашивает перед каждым действием
- **--dangerously-skip-permissions** — не рекомендуется без понимания рисков

### Slash-команды в сессии

\`\`\`
/help     — список доступных команд
/clear    — очистить контекст сессии
/compact  — сжать историю (сохранить токены)
/status   — показать статус текущей сессии
\`\`\`

### Выход из сессии

\`\`\`
/exit
# или Ctrl+C
\`\`\`

### Итог

- Запускайте Claude Code из корня проекта — так он видит весь контекст
- Начните с задач на понимание кода, потом переходите к изменениям
- Claude Code спрашивает разрешение перед изменениями — это норма
- \`/clear\` сбрасывает контекст, \`/compact\` его сжимает
`
  },
  {
    slug: 'prompt-discipline-intro',
    content: `## Дисциплина промптов — inputs определяют outputs

Качество результата от Claude Code напрямую зависит от качества задачи. Дисциплина промптов — это навык, который окупается быстро.

### Почему это важно

Claude Code — очень мощный инструмент, но он не читает мысли. Размытая задача → размытый результат. Конкретная задача → конкретный результат.

### Принципы хорошего промпта

**1. Описывай намерение, а не шаги**

❌ Плохо:
\`\`\`
Открой src/users.ts, найди функцию getUser, добавь параметр includeDeleted
\`\`\`

✅ Хорошо:
\`\`\`
Функция getUser должна принимать опциональный параметр includeDeleted: boolean.
Если он true — возвращать в том числе удалённых пользователей (поле deleted_at IS NOT NULL)
\`\`\`

**2. Давай контекст о том, почему**

\`\`\`
Добавь кэширование для запроса к /api/products.
Этот эндпоинт вызывается ~1000 раз/мин, данные меняются раз в час.
Используй Redis, который уже настроен в lib/redis.ts
\`\`\`

**3. Указывай ограничения и требования**

\`\`\`
Напиши функцию для парсинга CSV.
Требования:
- TypeScript
- Поддержка разных разделителей (,  ;  \t)
- Строки могут содержать переносы внутри кавычек
- Возвращать строго типизированный массив
\`\`\`

**4. Один запрос — одна задача**

Не смешивай несколько независимых задач в один промпт. Это усложняет ревью и повышает риск ошибки.

### Режим планирования

Для сложных задач сначала попроси план:
\`\`\`
Прежде чем писать код — опиши план реализации.
Жди моего подтверждения.
\`\`\`

Это позволяет поймать неправильное понимание задачи до того, как написаны сотни строк кода.

### Итеративные уточнения

Не нужно описывать всё сразу. Начните с базы:
\`\`\`
> Создай простой REST API для задач (Tasks)
> Хорошо, теперь добавь фильтрацию по статусу
> Теперь добавь пагинацию
\`\`\`

### Итог

- Описывай намерение и результат, а не пошаговые инструкции
- Давай контекст о причинах и ограничениях
- Один запрос — одна связная задача
- Для сложного — сначала план, потом реализация
`
  },
  {
    slug: 'planning-practice-intro',
    content: `## Планирование как ключевая практика

Планирование — это не формальность. Это способ убедиться, что Claude Code (и вы сами) правильно понимает задачу до того, как начнётся работа.

### Зачем планировать перед кодированием

Представьте: вы дали Claude Code задачу реализовать авторизацию. Он написал 500 строк кода. Вы открываете — и видите, что он реализовал JWT, хотя у вас уже была сессионная авторизация, которую нужно было только расширить.

Планирование предотвращает такие ситуации.

### Как попросить план

\`\`\`
Прежде чем писать код, составь план реализации следующего:
[описание задачи]

Включи:
- Какие файлы будут созданы/изменены
- Архитектурные решения и их обоснование
- Потенциальные риски

Жди моего подтверждения перед реализацией.
\`\`\`

### Пример диалога

\`\`\`
> Составь план добавления системы уведомлений. Не пиши код.

Claude Code:
1. Новая таблица notifications(id, user_id, type, payload, read_at, created_at)
2. API эндпоинты:
   - GET /api/notifications — список непрочитанных
   - POST /api/notifications/:id/read — отметить прочитанным
3. React-компонент NotificationBell — показывает счётчик
4. Polling каждые 30 сек (WebSocket добавим позже)
5. Файлы: migrations/004_notifications.sql, app/api/notifications/*, components/NotificationBell.tsx

Начать реализацию?

> Да, но используй SSE вместо polling для реального времени

Claude Code: Понял, обновляю план...
\`\`\`

### Plan Mode

В Claude Code есть встроенный Plan Mode (режим планирования). В этом режиме Claude Code только анализирует и предлагает план — не изменяет файлы.

Активируется через VS Code или командой \`/plan\`.

### Итерации плана

Не бойтесь итерировать план. Это гораздо дешевле, чем переписывать код:
- «А что если использовать X вместо Y?»
- «Можно ли обойтись без новой таблицы?»
- «Покажи альтернативный подход»

### Итог

- Планирование — это не потеря времени, а инвестиция
- Просите план явно: «Составь план, не пиши код, жди подтверждения»
- Итерируйте план в диалоге перед тем, как начать реализацию
- Plan Mode в Claude Code встроен для этой цели
`
  },
  {
    slug: 'building-by-features-intro',
    content: `## Построение по фичам

Один из наиболее эффективных паттернов работы с Claude Code — строить проект фича за фичей, не пытаясь сделать всё за один раз.

### Почему это работает

Claude Code хорошо справляется с ограниченными, чётко определёнными задачами. «Создай мне стартап» — слишком широко. «Создай страницу входа с формой email/пароль» — конкретная, выполнимая задача.

Разбивая проект на фичи, вы:
- Получаете рабочий результат после каждого шага
- Можете проверить качество, пока контекст ещё свеж
- Сохраняете контроль над архитектурой
- Легко откатываете отдельный шаг, если что-то пошло не так

### Структура разработки фичи

Типичный цикл работы над фичей:

\`\`\`
1. Описать фичу и её требования
2. Попросить план (если фича нетривиальная)
3. Утвердить план
4. Выполнение (Claude Code пишет код)
5. Проверить результат: открыть в браузере, запустить тесты
6. Зафиксировать (git commit)
7. Перейти к следующей фиче
\`\`\`

### Пример: строим курсовую платформу

❌ Неэффективно:
\`\`\`
Создай платформу для онлайн-курсов с видео, тестами, прогрессом и сертификатами
\`\`\`

✅ По фичам:
\`\`\`
Шаг 1: Создай базовую структуру Next.js проекта с PostgreSQL
Шаг 2: Добавь страницу со списком курсов
Шаг 3: Добавь страницу урока с Markdown-контентом
Шаг 4: Добавь кнопку "Урок пройден" с localStorage
...
\`\`\`

### Коммиты после каждой фичи

\`\`\`bash
# После каждой завершённой фичи
git add -p
git commit -m "Add lesson page with markdown content"
\`\`\`

Или попросите Claude Code сделать это:
\`\`\`
Сделай git commit с понятным сообщением о том, что было сделано
\`\`\`

### Когда фича слишком большая

Если Claude Code начинает делать слишком много изменений — стоп. Разбейте фичу на подзадачи.

Признаки того, что фича слишком большая:
- Claude Code меняет больше 10 файлов
- Сессия затягивается и контекст пухнет
- Вы теряете понимание, что именно делается

### Итог

- Разбивайте проект на фичи с чёткими границами
- Каждая фича: план → реализация → проверка → коммит
- Если фича разрослась — стоп, делите дальше
- Коммиты после каждой фичи — ваша страховка
`
  },
  {
    slug: 'how-claude-code-works-intro',
    content: `## Как на самом деле работает Claude Code

Понимание внутреннего устройства Claude Code помогает предсказывать его поведение и эффективнее им управлять.

### Агентный цикл

Claude Code работает в цикле: воспринимает окружение → думает → действует → снова воспринимает.

\`\`\`
┌──────────────────────────────────────┐
│  Пользователь даёт задачу             │
└─────────────────┬────────────────────┘
                  ↓
┌──────────────────────────────────────┐
│  Claude анализирует задачу            │
│  + текущий контекст (файлы, история) │
└─────────────────┬────────────────────┘
                  ↓
┌──────────────────────────────────────┐
│  Выбирает инструмент и параметры     │
│  (Read, Edit, Bash, WebSearch...)    │
└─────────────────┬────────────────────┘
                  ↓
┌──────────────────────────────────────┐
│  Выполняет действие                  │
│  Видит результат                     │
└─────────────────┬────────────────────┘
                  ↓
        ┌─────────────────┐
        │  Задача решена?  │
        └────┬────────────┘
           Нет ↓         Да ↓
        Следующий   Ответ пользователю
        шаг цикла
\`\`\`

### Инструменты Claude Code

Claude Code использует набор встроенных инструментов:

| Инструмент | Что делает |
|-----------|-----------|
| Read | Читает файл или директорию |
| Edit / Write | Редактирует или создаёт файл |
| Bash | Выполняет команду в терминале |
| Glob | Ищет файлы по паттерну |
| Grep | Ищет паттерн в файлах |
| WebSearch | Ищет в интернете |
| WebFetch | Загружает страницу |
| Agent | Запускает субагента |
| TodoWrite | Управляет списком задач |

### Контекстное окно и память

Claude Code хранит в контексте:
- Системный промпт (внутренние инструкции)
- Содержимое CLAUDE.md
- Историю диалога
- Файлы, которые читались в этой сессии
- Вывод выполненных команд

Когда контекст приближается к лимиту, Claude Code автоматически сжимает историю (compaction).

### Разрешения (Permissions)

По умолчанию Claude Code спрашивает разрешение перед:
- Изменением файлов
- Выполнением команд
- Сетевыми запросами

Разрешения можно настроить в \`~/.claude/settings.json\`:
\`\`\`json
{
  "permissions": {
    "allow": ["Read", "Edit", "Bash(git *)"],
    "deny": ["Bash(rm -rf *)"]
  }
}
\`\`\`

### Системный промпт и CLAUDE.md

При каждом запуске Claude Code загружает:
1. Встроенный системный промпт (определяет его базовое поведение)
2. CLAUDE.md из текущего проекта (ваши правила проекта)
3. ~/.claude/CLAUDE.md (ваши глобальные правила)

### Итог

- Claude Code работает в цикле: воспринять → думать → действовать → повторить
- Использует набор инструментов для взаимодействия с системой
- Контекст ограничен — длинные сессии накапливают «мусор»
- Поведение настраивается через CLAUDE.md и settings.json
`
  },
  {
    slug: 'app-architecture-intro',
    content: `## Архитектура приложений с Claude Code

Claude Code хорошо понимает современные архитектурные паттерны. Правильная структура проекта помогает ему работать эффективнее.

### Что Claude Code любит

Claude Code работает лучше всего с проектами, где:
- Есть чёткое разделение слоёв (UI, бизнес-логика, данные)
- Используются стандартные паттерны (MVC, Repository, Service Layer)
- Файлы организованы по feature или layer, не по типу
- TypeScript с явными типами

### Структура Next.js проекта (пример)

\`\`\`
my-app/
├── app/                    # App Router страницы
│   ├── api/               # API Routes
│   ├── (auth)/            # Route group
│   └── layout.tsx
├── components/            # React компоненты
│   ├── ui/               # Переиспользуемые UI-элементы
│   └── features/         # Фичевые компоненты
├── lib/                   # Утилиты, клиенты
│   ├── db.ts             # Подключение к БД
│   └── queries.ts        # SQL-запросы
├── hooks/                 # Custom hooks
├── types/                 # TypeScript типы
│   └── index.ts
├── scripts/               # Служебные скрипты
└── CLAUDE.md             # Контекст для Claude
\`\`\`

### Роль CLAUDE.md в архитектуре

CLAUDE.md — это место, где вы описываете архитектурные решения проекта. Claude Code читает его при каждом запуске:

\`\`\`markdown
## Архитектура

- БД: PostgreSQL через lib/db.ts (pg Pool, singleton)
- API: App Router API Routes в app/api/
- Типы: централизованы в types/index.ts
- Состояние: localStorage (авторизации нет)

## Правила

- Не использовать any в TypeScript
- SQL-запросы только параметризованные ($1, $2)
- Компоненты: 'use client' только где нужен стейт/эффекты
\`\`\`

### Паттерны, которые Claude Code знает

- **Repository pattern** — отделяет логику работы с данными
- **Service layer** — бизнес-логика отдельно от HTTP
- **Feature-based** — код по фичам, а не по типам файлов
- **Server/Client split** (Next.js) — правильное разделение серверных и клиентских компонентов

### Рефакторинг с Claude Code

Если проект уже существует с хаотичной структурой:
\`\`\`
Изучи текущую структуру проекта.
Предложи план рефакторинга для перехода на feature-based структуру.
Не меняй файлы, только план.
\`\`\`

### Итог

- Стандартная структура проекта = лучший контекст для Claude Code
- CLAUDE.md фиксирует архитектурные решения
- Claude Code понимает паттерны: Repository, Service Layer, Feature-based
- При рефакторинге — сначала план, потом действие
`
  },
  {
    slug: 'plan-edit-modes-intro',
    content: `## Plan Mode, Edit Mode и операционные режимы

Claude Code поддерживает разные режимы работы, каждый из которых оптимизирован под конкретный тип задачи.

### Plan Mode (режим планирования)

В Plan Mode Claude Code только анализирует и планирует — не изменяет файлы. Это безопасный режим для изучения задачи.

**Когда использовать:**
- Перед крупными рефакторингами
- При разработке новой фичи
- Когда хотите понять последствия изменений перед их применением

**Активация:**
- В VS Code: специальная кнопка в панели Claude Code
- Команда \`/plan\` в сессии

В Plan Mode Claude Code:
- Читает файлы
- Анализирует зависимости
- Составляет план действий
- Спрашивает подтверждения

### Edit Mode (режим редактирования)

Стандартный режим работы. Claude Code может читать и изменять файлы, выполнять команды (с вашего разрешения).

### Auto-режим (--auto-approve)

Для полностью автономной работы без запроса разрешений. Использовать только если полностью доверяете задаче и понимаете последствия.

\`\`\`bash
claude --auto-approve "Запусти все тесты и исправь упавшие"
\`\`\`

### Настройка разрешений

В \`~/.claude/settings.json\` можно тонко настроить, что Claude Code делает без запроса:

\`\`\`json
{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(npm test)"
    ],
    "deny": [
      "Bash(rm *)",
      "Bash(git push *)"
    ]
  }
}
\`\`\`

### Проектные настройки

Кроме глобальных, можно задать настройки на уровне проекта в \`.claude/settings.json\`:

\`\`\`json
{
  "permissions": {
    "allow": ["Bash(npm run *)"]
  }
}
\`\`\`

Настройки проекта объединяются с глобальными.

### Переключение моделей

\`\`\`bash
# В сессии
/model claude-opus-4-7

# При запуске
claude --model claude-haiku-4-5
\`\`\`

### Итог

- Plan Mode — безопасный анализ без изменений
- Edit Mode — стандартная работа с подтверждениями
- Auto-режим — полная автономия (осторожно)
- Настройки разрешений — точечный контроль над тем, что Claude Code делает автоматически
`
  },
  {
    slug: 'context-windows-sessions-intro',
    content: `## Контекстные окна и управление сессиями

Контекстное окно — главое ограничение Claude Code. Понимание того, как оно работает, помогает работать эффективнее.

### Что такое контекстное окно

Контекстное окно — это «оперативная память» Claude Code за одну сессию. Всё, что в нём помещается:
- Системный промпт
- Содержимое CLAUDE.md
- История диалога
- Прочитанные файлы
- Вывод команд

Размер окна у Claude: **200 000 токенов**. Это примерно 150 000 слов или ~500 страниц текста.

### Проблема накопления контекста

По мере работы контекст заполняется. Длинная сессия с большим количеством файлов может исчерпать окно. Симптомы:
- Claude Code начинает «забывать» ранние части разговора
- Качество ответов снижается
- Claude Code сообщает о приближении лимита

### Управление контекстом

**\`/clear\` — полная очистка**
\`\`\`
/clear
\`\`\`
Сбрасывает всю историю. Claude Code забывает всё, что было в сессии. Начинайте с этой команды, если переключаетесь на новую задачу.

**\`/compact\` — сжатие истории**
\`\`\`
/compact
\`\`\`
Claude Code суммирует историю, сохраняя ключевые решения. Контекст уменьшается, но важное остаётся.

### Автоматический compact

Когда контекст достигает ~80% заполнения, Claude Code автоматически сжимает историю. Вы увидите сообщение:
\`\`\`
Context is getting long. Compacting conversation history...
\`\`\`

### Стратегия управления сессиями

Для больших проектов рекомендуется:
1. **Одна сессия — одна задача.** Не тащите в одну сессию несвязанные задачи.
2. **\`/clear\` при смене задачи.** Не нагромождайте контекст.
3. **CLAUDE.md для постоянного контекста.** То, что должно быть всегда — кладите в CLAUDE.md.
4. **Коммиты как чекпоинты.** Закоммитили → можно начать новую сессию.

### Продолжение прерванной сессии

\`\`\`bash
claude --resume  # Возобновить последнюю сессию
\`\`\`

### Итог

- Контекстное окно: 200K токенов — много, но конечно
- \`/clear\` — полный сброс для новой задачи
- \`/compact\` — сжатие при сохранении ключевых моментов
- Один сессии — одна задача; CLAUDE.md — для постоянного контекста
`
  },
  {
    slug: 'mcp-servers-integrations-intro',
    content: `## MCP-серверы и внешние интеграции

Model Context Protocol (MCP) — открытый стандарт, который позволяет Claude Code подключаться к внешним инструментам и данным.

### Что такое MCP

MCP (Model Context Protocol) — протокол, разработанный Anthropic, для унифицированного подключения ИИ-агентов к внешним сервисам. Вместо того чтобы писать уникальные интеграции для каждого инструмента, MCP предоставляет стандартный интерфейс.

Архитектура MCP:
\`\`\`
Claude Code ←→ MCP Protocol ←→ MCP Server ←→ Внешний сервис
                                              (GitHub, Jira, БД, API...)
\`\`\`

### MCP инструменты vs MCP ресурсы

**Tools (инструменты)** — действия, которые Claude может выполнять:
- Создать issue в GitHub
- Запросить данные из базы
- Отправить сообщение в Slack

**Resources (ресурсы)** — данные, которые Claude может читать:
- Документация (контекст без передачи в промпт)
- Схема базы данных
- Конфигурационные файлы

### Настройка MCP-серверов

MCP-серверы настраиваются в \`~/.claude/mcp.json\` (глобально) или \`.claude/mcp.json\` (для проекта):

\`\`\`json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_..."
      }
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"],
      "env": {
        "POSTGRES_CONNECTION_STRING": "postgresql://..."
      }
    }
  }
}
\`\`\`

### Использование в сессии

После настройки MCP-серверы доступны автоматически:
\`\`\`
> Создай GitHub issue для бага который мы только что нашли

> Покажи последние 10 записей из таблицы users

> Отправь в Slack уведомление о деплое
\`\`\`

### Популярные MCP-серверы

- **@modelcontextprotocol/server-github** — GitHub Issues, PR, репозитории
- **@modelcontextprotocol/server-postgres** — PostgreSQL запросы
- **@modelcontextprotocol/server-filesystem** — расширенная работа с файлами
- **@modelcontextprotocol/server-brave-search** — поиск через Brave
- **@modelcontextprotocol/server-memory** — персистентная память

### Итог

- MCP — стандарт для подключения Claude к внешним сервисам
- Настраивается в .claude/mcp.json
- Tools — действия, Resources — данные для чтения
- После настройки работает прозрачно в любой сессии
`
  },
  {
    slug: 'mcp-server-python-docker',
    content: `## Как создать MCP-сервер с Python, Docker и Claude Code

Создание собственного MCP-сервера позволяет подключить Claude Code к любому внутреннему инструменту или API.

### Структура MCP-сервера

MCP-сервер — это процесс, который общается с Claude Code по стандартному протоколу через stdin/stdout. Может быть написан на любом языке.

Минимальная структура Python MCP-сервера:

\`\`\`python
# server.py
from mcp.server import Server
from mcp.server.stdio import stdio_server
from mcp import types

server = Server("my-custom-server")

@server.list_tools()
async def list_tools() -> list[types.Tool]:
    return [
        types.Tool(
            name="get_metrics",
            description="Получить метрики приложения",
            inputSchema={
                "type": "object",
                "properties": {
                    "service": {"type": "string", "description": "Имя сервиса"}
                },
                "required": ["service"]
            }
        )
    ]

@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[types.TextContent]:
    if name == "get_metrics":
        service = arguments["service"]
        # Ваша логика здесь
        metrics = fetch_metrics(service)
        return [types.TextContent(type="text", text=str(metrics))]

async def main():
    async with stdio_server() as streams:
        await server.run(*streams, server.create_initialization_options())

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
\`\`\`

### Зависимости

\`\`\`bash
pip install mcp anthropic
\`\`\`

\`\`\`toml
# pyproject.toml
[project]
dependencies = ["mcp>=1.0.0"]
\`\`\`

### Упаковка в Docker

\`\`\`dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY server.py .
CMD ["python", "server.py"]
\`\`\`

### Регистрация в Claude Code

\`\`\`json
// .claude/mcp.json
{
  "mcpServers": {
    "my-metrics": {
      "command": "docker",
      "args": ["run", "--rm", "-i", "my-metrics-server"],
      "env": {
        "METRICS_API_KEY": "your-key"
      }
    }
  }
}
\`\`\`

Или для локального Python скрипта:
\`\`\`json
{
  "mcpServers": {
    "my-metrics": {
      "command": "python",
      "args": ["/path/to/server.py"]
    }
  }
}
\`\`\`

### Использование Claude Code для создания MCP

Интересно: можно попросить Claude Code написать сам MCP-сервер:

\`\`\`
Создай MCP-сервер на Python, который:
1. Подключается к нашему внутреннему API (base URL в env: INTERNAL_API_URL)
2. Предоставляет tool get_deployment_status(service: str)
3. Предоставляет tool trigger_deploy(service: str, version: str)
\`\`\`

### Итог

- MCP-сервер = процесс на stdin/stdout, реализующий протокол MCP
- Python + библиотека mcp — наиболее удобный способ
- Docker упрощает изоляцию и деплой сервера
- Регистрация в .claude/mcp.json, работает в любой сессии проекта
`
  },
  {
    slug: 'mcp-top-servers',
    content: `## Топ MCP-серверов, которые превращают Claude в машину

Экосистема MCP-серверов быстро растёт. Вот наиболее ценные серверы для разработчиков.

### GitHub

\`\`\`json
{
  "github": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_..." }
  }
}
\`\`\`

**Возможности:** создавать/читать issues и PR, комментировать, управлять ветками.

\`\`\`
> Создай PR из текущей ветки в main с описанием изменений
> Покажи все open issues с лейблом "bug"
\`\`\`

### PostgreSQL / базы данных

\`\`\`json
{
  "postgres": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-postgres"],
    "env": { "POSTGRES_CONNECTION_STRING": "postgresql://..." }
  }
}
\`\`\`

**Возможности:** SQL-запросы, просмотр схемы, анализ данных.

\`\`\`
> Покажи структуру таблицы users
> Сколько пользователей зарегистрировалось за последние 7 дней?
\`\`\`

### Filesystem (расширенный)

\`\`\`json
{
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "/allowed/path"]
  }
}
\`\`\`

**Возможности:** чтение файлов за пределами проекта (документация, конфиги).

### Brave Search

\`\`\`json
{
  "brave-search": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-brave-search"],
    "env": { "BRAVE_API_KEY": "BSA..." }
  }
}
\`\`\`

**Возможности:** поиск в интернете с высоким качеством результатов.

### Memory (персистентная память)

\`\`\`json
{
  "memory": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-memory"]
  }
}
\`\`\`

**Возможности:** Claude может сохранять информацию между сессиями.

\`\`\`
> Запомни: наш production API endpoint — api.example.com, staging — staging.api.example.com
\`\`\`

### Slack

\`\`\`json
{
  "slack": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-slack"],
    "env": {
      "SLACK_BOT_TOKEN": "xoxb-...",
      "SLACK_TEAM_ID": "T..."
    }
  }
}
\`\`\`

**Возможности:** отправка сообщений, чтение каналов.

### Как найти новые серверы

- **Официальный репозиторий:** github.com/modelcontextprotocol/servers
- **MCP Hub:** mcphub.io

### Итог

- GitHub MCP — незаменим для workflow с issues и PR
- PostgreSQL MCP — позволяет Claude работать с данными напрямую
- Memory MCP — даёт постоянную память между сессиями
- Любой внешний сервис можно подключить через собственный MCP-сервер
`
  },
  {
    slug: 'agents-subagents-parallel-intro',
    content: `## Агенты, субагенты и параллельные воркфлоу

Claude Code может запускать субагентов — независимые копии Claude, которые выполняют подзадачи параллельно.

### Что такое субагент

Субагент — это отдельный экземпляр Claude с собственным контекстом, запущенный для выполнения конкретной подзадачи. Он работает независимо и возвращает результат родительскому агенту.

### Зачем нужны субагенты

**Параллельная работа.** Вместо последовательного выполнения задач A → B → C, субагенты позволяют выполнять A, B, C одновременно.

**Изоляция контекста.** Каждый субагент начинает с чистым контекстом. Это полезно, когда задачи не должны влиять друг на друга.

**Специализация.** Разные субагенты могут быть оптимизированы под разные задачи (разные модели, настройки).

### Как это работает в Claude Code

Claude Code автоматически создаёт субагентов для параллельных задач:

\`\`\`
> Проведи ревью кода: проверь security уязвимости,
  производительность и соответствие style guide.
  Делай параллельно.
\`\`\`

Claude Code может запустить три субагента одновременно, каждый фокусируется на своём аспекте.

### SDK для субагентов (Claude Agent SDK)

При разработке собственных агентов на базе Claude:

\`\`\`typescript
import Anthropic from '@anthropic-ai/sdk'

const client = new Anthropic()

// Запуск субагента
const result = await client.messages.create({
  model: 'claude-sonnet-4-6',
  max_tokens: 4096,
  tools: [...], // инструменты субагента
  messages: [
    { role: 'user', content: 'Проанализируй безопасность этого кода: ...' }
  ]
})
\`\`\`

### Параллельные воркфлоу

Классический сценарий: обработка данных в параллельных потоках.

\`\`\`
> Нам нужно обработать 100 файлов в папке /data.
  Каждый файл нужно:
  1. Провалидировать
  2. Трансформировать
  3. Загрузить в БД

  Используй параллельную обработку, по 10 файлов одновременно.
\`\`\`

### Ограничения

- Субагенты увеличивают потребление токенов (каждый имеет свой контекст)
- Субагенты не могут напрямую общаться между собой
- Координация возвращается родительскому агенту

### Итог

- Субагенты — независимые экземпляры Claude для параллельных задач
- Claude Code автоматически использует их для параллельной работы
- Полезны для: параллельного ревью, обработки данных, специализированных задач
- SDK доступен для создания собственных агентных систем
`
  },
  {
    slug: 'skills-rules-persistent-intro',
    content: `## Skills, правила и персистентные инструкции

Skills — это заготовленные команды для Claude Code. Rules и CLAUDE.md — постоянные инструкции, которые работают в каждой сессии.

### CLAUDE.md — главный механизм персистентных инструкций

CLAUDE.md — это файл, который Claude Code читает при каждом запуске. Храните в нём всё, что Claude должен знать о проекте:

\`\`\`markdown
# My Project

## Стек
- Next.js 14 App Router
- PostgreSQL через lib/db.ts

## Правила кода
- TypeScript, строгий режим
- Tailwind CSS для стилей
- Все SQL-запросы параметризованные

## Команды
- npm run dev — запуск разработки
- npm run test — тесты
- npm run build — сборка
\`\`\`

### Иерархия CLAUDE.md

\`\`\`
~/.claude/CLAUDE.md          # Глобальные правила (все проекты)
~/project/CLAUDE.md          # Правила проекта
~/project/src/CLAUDE.md      # Правила для поддиректории
\`\`\`

Инструкции объединяются: сначала глобальные, потом проектные, потом директории.

### Skills — заготовленные команды

Skills — это шаблонные команды, вызываемые через \`/skill-name\`. Хранятся в \`.claude/skills/\` или \`~/.claude/skills/\`.

Пример skill для ревью PR:

\`\`\`markdown
---
name: review-pr
description: Провести ревью текущих изменений
---

Проверь git diff и сделай code review:
1. Безопасность: SQL injection, XSS, открытые секреты
2. Производительность: N+1 запросы, лишние ре-рендеры
3. Типобезопасность: any, несовпадения типов
4. Тесты: покрытие новой функциональности

Формат: краткое резюме + список проблем по приоритету
\`\`\`

Вызов: \`/review-pr\`

### .claude/rules/ — модульные правила

Альтернатива большому CLAUDE.md — разбить правила на файлы:

\`\`\`
.claude/
├── rules/
│   ├── typescript.md     # Правила TypeScript
│   ├── testing.md        # Правила тестирования
│   └── git.md            # Правила git commits
└── skills/
    └── review-pr.md
\`\`\`

### Глобальные vs проектные инструкции

**Глобальные (~/.claude/CLAUDE.md):**
- Личные предпочтения (язык ответов, стиль)
- Общие правила для всех проектов

**Проектные (./CLAUDE.md):**
- Стек и архитектура конкретного проекта
- Специфичные команды и скрипты
- Правила команды

### Итог

- CLAUDE.md — главный способ задать постоянный контекст
- Skills — заготовленные команды, вызываются через /имя
- Иерархия: глобальные → проектные → директории
- .claude/rules/ — модульная организация правил
`
  },
  {
    slug: 'skills-create-custom',
    content: `## Как создать свой собственный Claude Code Skill

Skill — это Markdown-файл с frontmatter, который превращается в slash-команду. Создание навыков экономит время на повторяющихся задачах.

### Структура Skill-файла

\`\`\`markdown
---
name: skill-name
description: Что делает этот skill (используется в /help)
---

Инструкции для Claude Code...
\`\`\`

Frontmatter обязателен: \`name\` и \`description\`. Тело файла — это промпт.

### Где хранить skills

\`\`\`
~/.claude/skills/          # Глобальные (все проекты)
.claude/skills/            # Только текущий проект
\`\`\`

### Пример: skill для создания компонента

\`\`\`markdown
---
name: component
description: Создать React компонент с TypeScript и Tailwind
---

Создай React компонент по следующим требованиям:
- TypeScript с явными типами для props
- Tailwind CSS для стилей (без inline styles)
- 'use client' только если нужен state или эффекты
- Экспортировать как default export
- Файл: components/{{name}}.tsx

Имя компонента: {{args}}

Если имя не указано, спроси перед созданием.
\`\`\`

Вызов: \`/component UserCard\`

### Пример: skill для git commit

\`\`\`markdown
---
name: commit
description: Создать git commit с хорошим сообщением
---

Посмотри git diff --staged и создай коммит:
1. Если ничего не staged — запусти git add -p и предложи мне выбрать
2. Составь сообщение: тип(scope): описание на русском
   - Типы: feat, fix, refactor, docs, test, chore
3. Не включай технические детали реализации в сообщение
4. Запусти git commit с этим сообщением
\`\`\`

Вызов: \`/commit\`

### Аргументы в skills

Аргументы доступны через \`{{args}}\` в теле skill:

\`\`\`markdown
---
name: explain
description: Объяснить функцию или файл
---

Найди и объясни: {{args}}

Формат объяснения:
1. Что делает (одна строка)
2. Входные данные и их типы
3. Что возвращает
4. Возможные ошибки
5. Где используется в проекте
\`\`\`

Вызов: \`/explain processPayment\`

### Список доступных skills

\`\`\`
/help  — показывает все доступные skills с описаниями
\`\`\`

### Итог

- Skill = Markdown-файл с frontmatter в .claude/skills/ или ~/.claude/skills/
- Обязательные поля: name, description
- Аргументы доступны через {{args}}
- Вызов: /имя-навыка [аргументы]
- /help показывает все доступные skills
`
  },
  {
    slug: 'skills-claude-folder-anatomy',
    content: `## Анатомия папки .claude/

Папка .claude/ — это конфигурационный центр Claude Code для конкретного проекта. Разберём каждый элемент.

### Полная структура .claude/

\`\`\`
.claude/
├── settings.json          # Настройки разрешений и поведения
├── mcp.json              # MCP-серверы для проекта
├── skills/               # Кастомные slash-команды
│   ├── component.md
│   ├── commit.md
│   └── review.md
├── rules/                # Модульные правила (альтернатива CLAUDE.md)
│   ├── typescript.md
│   └── testing.md
└── memory/               # Автоматическая память (создаётся Claude)
    ├── MEMORY.md         # Индекс воспоминаний
    └── user_prefs.md     # Конкретные воспоминания
\`\`\`

### settings.json

\`\`\`json
{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "Bash(npm run *)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git status)"
    ],
    "deny": [
      "Bash(git push *)",
      "Bash(rm -rf *)"
    ]
  },
  "model": "claude-sonnet-4-6",
  "env": {
    "NODE_ENV": "development"
  }
}
\`\`\`

### mcp.json

\`\`\`json
{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"],
      "env": {
        "POSTGRES_CONNECTION_STRING": "postgresql://localhost/mydb"
      }
    }
  }
}
\`\`\`

### Коммитить или нет?

| Файл | Коммитить? |
|------|-----------|
| settings.json | Да (без секретов) |
| mcp.json | Осторожно (не коммитить API ключи) |
| skills/ | Да |
| rules/ | Да |
| memory/ | Нет (.gitignore) |

Рекомендуемый .gitignore:
\`\`\`
.claude/memory/
.claude/mcp.json  # если содержит ключи
\`\`\`

### Глобальная папка ~/.claude/

Аналогичная структура, но для всех проектов:
\`\`\`
~/.claude/
├── settings.json    # Глобальные настройки
├── CLAUDE.md        # Глобальные инструкции
├── skills/          # Глобальные skills
└── memory/          # Глобальная память
\`\`\`

Проектные настройки переопределяют/дополняют глобальные.

### Итог

- .claude/ — конфигурационный центр проекта для Claude Code
- settings.json — разрешения и модель
- mcp.json — подключение внешних сервисов
- skills/ — slash-команды проекта
- memory/ — в .gitignore
`
  },
  {
    slug: 'autonomous-loops-intro',
    content: `## Автономные циклы — условия применения

Claude Code может работать в полностью автономном режиме — выполнять задачи без вашего участия. Когда это оправдано?

### Что такое автономный цикл

Автономный цикл (/loop или --auto-approve) — режим, в котором Claude Code:
- Сам принимает решения о следующих шагах
- Выполняет команды без подтверждения
- Продолжает работать до завершения задачи или ошибки

### Когда это безопасно использовать

✅ **Подходящие задачи:**
- Запуск тест-сьюта и исправление всех упавших тестов
- Рефакторинг по чётким правилам (например, замена deprecated API)
- Генерация boilerplate кода по шаблону
- Обработка/трансформация данных в файлах

✅ **Условия безопасности:**
- Задача чётко определена и ограничена
- Есть возможность откатить (git clean, бэкапы)
- Изменения только локальные (не production)
- Понятный критерий завершения

### Когда НЕ использовать

❌ **Опасные сценарии:**
- Деплой в production
- Удаление данных
- Изменение прав доступа
- Отправка запросов к внешним API без ревью

### Пример: автономный запуск тестов

\`\`\`bash
claude --auto-approve "Запусти npm test. Если есть упавшие тесты —
исправь их. Не меняй логику приложения, только тесты.
Продолжай пока все тесты не пройдут или пока не упрёшься в
нерешаемую проблему."
\`\`\`

### /loop — динамические автономные циклы

\`\`\`
/loop Мониторь файл logs/error.log и при появлении новых ошибок
анализируй их и создавай issues в GitHub
\`\`\`

### Настройка автоматических разрешений

Вместо полного auto-approve — разрешить конкретные операции:

\`\`\`json
// .claude/settings.json
{
  "permissions": {
    "allow": [
      "Bash(npm test)",
      "Bash(npm run lint)",
      "Edit",
      "Read"
    ],
    "deny": ["Bash(git push *)", "Bash(rm *)"]
  }
}
\`\`\`

Так Claude Code может автономно запускать тесты и редактировать файлы, но не пушить и не удалять.

### Итог

- Автономные циклы мощны, но требуют чётко определённой и ограниченной задачи
- Всегда иметь возможность отката перед запуском (git stash, бэкап)
- Лучше разрешать конкретные операции, чем включать полный auto-approve
- Никогда не запускайте автономный цикл с доступом к production-данным
`
  },
  {
    slug: 'code-review-security-intro',
    content: `## Code Review, безопасность и верификация

Claude Code — мощный инструмент для code review. Правильно поставленная задача даёт глубокий анализ, который дополняет автоматические линтеры.

### Code Review с Claude Code

\`\`\`bash
# Ревью текущих изменений
claude "Сделай code review git diff. Фокус на: безопасность,
производительность, потенциальные баги"

# Ревью конкретного файла
claude "Проверь src/auth/jwt.ts на безопасность и best practices"

# Ревью перед PR
claude "Мы готовимся смерджить эту ветку. Проверь все изменения
относительно main и укажи на что обратить внимание"
\`\`\`

### Типичные уязвимости, которые найдёт Claude

**SQL Injection:**
\`\`\`typescript
// ❌ Уязвимо
const result = await db.query(\`SELECT * FROM users WHERE id = \${userId}\`)

// ✅ Безопасно
const result = await db.query('SELECT * FROM users WHERE id = $1', [userId])
\`\`\`

**XSS:**
\`\`\`tsx
// ❌ Уязвимо
<div dangerouslySetInnerHTML={{ __html: userContent }} />

// ✅ Безопасно — React экранирует автоматически
<div>{userContent}</div>
\`\`\`

**Открытые секреты:**
\`\`\`typescript
// ❌ Никогда
const apiKey = "sk-live-abc123"

// ✅ Через переменные окружения
const apiKey = process.env.API_KEY
\`\`\`

### Skill для систематического ревью

\`\`\`markdown
---
name: security-review
description: Полный security review изменений
---

Проведи security review git diff HEAD:

**КРИТИЧНО (блокирует merge):**
- SQL/NoSQL injection
- XSS возможности
- Открытые секреты/ключи в коде
- Сломанная аутентификация/авторизация

**ВАЖНО (нужно исправить):**
- Небезопасная десериализация
- Отсутствие валидации пользовательского ввода
- Небезопасные прямые ссылки на объекты

**РЕКОМЕНДАЦИИ:**
- Логирование критических операций
- Rate limiting для публичных эндпоинтов

Для каждой проблемы: файл:строка, описание, пример исправления.
\`\`\`

### Верификация решений Claude Code

Claude Code может ошибаться. Паттерны для верификации:

\`\`\`
> Ты только что написал функцию processPayment. Проверь её сам:
  1. Обработаны ли все edge cases?
  2. Что произойдёт если payment gateway недоступен?
  3. Корректно ли обрабатываются транзакции БД?
\`\`\`

### Итог

- Claude Code находит SQL injection, XSS, открытые секреты
- Создайте skill /security-review для систематической проверки
- Просите Claude Code самому верифицировать своё решение
- Автоматические линтеры + Claude = дополняющие слои защиты
`
  },
  {
    slug: 'starter-project-templates-intro',
    content: `## Шаблоны стартовых проектов

Один из самых эффективных способов использовать Claude Code — генерация проектов с нуля. Правильный промпт создаёт готовую к работе базу.

### Принцип: описывай результат, не шаги

\`\`\`
Создай Next.js 14 проект с:
- App Router
- TypeScript в строгом режиме
- Tailwind CSS
- PostgreSQL через pg (Pool singleton)
- Базовая структура: app/, components/, lib/, types/, hooks/
- CLAUDE.md с описанием проекта
- .env.example с нужными переменными

Не объясняй — просто создай файлы.
\`\`\`

### Шаблон: REST API на Node.js + Express

\`\`\`
Создай Express.js API сервер:
- TypeScript
- PostgreSQL через pg
- JWT аутентификация (без сторонних библиотек)
- Структура: src/routes/, src/middleware/, src/db/
- Endpoints: POST /auth/login, GET /users (protected)
- Dockerfile для деплоя
- package.json со скриптами dev, build, start
\`\`\`

### Шаблон: CLI-инструмент

\`\`\`
Создай CLI-инструмент на Node.js:
- TypeScript + tsx для запуска
- Команды: init, add, list, remove
- Конфиг в ~/.toolname/config.json
- Красивый вывод через chalk
- --help для каждой команды
\`\`\`

### Стартовый CLAUDE.md для любого проекта

Попросите Claude Code создать CLAUDE.md сразу:

\`\`\`
После создания проекта — создай CLAUDE.md, который описывает:
1. Стек и зависимости
2. Структуру директорий и зачем каждая
3. Как запустить dev, тесты, build
4. Ключевые архитектурные решения
5. Правила кода для этого проекта
\`\`\`

### Работа с существующим проектом

Если проект уже существует:

\`\`\`
Изучи этот проект. Создай CLAUDE.md который поможет
любому разработчику (и тебе в следующих сессиях)
быстро понять как здесь всё устроено.
\`\`\`

### Итог

- Claude Code генерирует полные проекты из одного описания
- Описывайте стек, структуру и требования — не шаги по созданию
- Всегда просите создать CLAUDE.md вместе с проектом
- Для существующих проектов — попросите Claude Code написать CLAUDE.md
`
  },
  {
    slug: 'starter-hooks-automation',
    content: `## 8 хуков Claude Code, которые автоматизируют всю твою рутину

Hooks — это shell-команды, которые автоматически запускаются в ответ на события в Claude Code. Настраивают поведение без изменения промптов.

### Что такое хуки

Хуки выполняются в определённые моменты работы Claude Code:

| Событие | Когда |
|---------|-------|
| PreToolUse | Перед каждым вызовом инструмента |
| PostToolUse | После вызова инструмента |
| PreCompact | Перед сжатием контекста |
| Stop | При завершении ответа агента |
| Notification | При уведомлениях |

### Настройка хуков

В \`~/.claude/settings.json\` или \`.claude/settings.json\`:

\`\`\`json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint --silent 2>&1 | head -20"
          }
        ]
      }
    ]
  }
}
\`\`\`

### 8 полезных хуков

**1. Автолинт после редактирования**
\`\`\`json
{
  "matcher": "Edit|Write",
  "hooks": [{ "type": "command", "command": "npm run lint --fix 2>&1" }]
}
\`\`\`

**2. Автоформатирование (Prettier)**
\`\`\`json
{
  "matcher": "Write",
  "hooks": [{ "type": "command", "command": "npx prettier --write $TOOL_INPUT_PATH 2>&1" }]
}
\`\`\`

**3. Проверка типов после изменений TS**
\`\`\`json
{
  "matcher": "Edit",
  "hooks": [{ "type": "command", "command": "npx tsc --noEmit 2>&1 | head -30" }]
}
\`\`\`

**4. Запуск тестов для изменённого файла**
\`\`\`json
{
  "matcher": "Edit",
  "hooks": [{ "type": "command", "command": "npm test -- --testPathPattern=$TOOL_INPUT_PATH --passWithNoTests 2>&1" }]
}
\`\`\`

**5. Уведомление о завершении (macOS)**
\`\`\`json
{
  "event": "Stop",
  "hooks": [{ "type": "command", "command": "osascript -e 'display notification \"Claude Code завершил задачу\" with title \"Claude Code\"'" }]
}
\`\`\`

**6. Блокировка опасных команд**
\`\`\`json
{
  "matcher": "Bash",
  "hooks": [{
    "type": "command",
    "command": "echo $TOOL_INPUT_CMD | grep -qE '^rm -rf|DROP TABLE' && echo 'BLOCKED' && exit 1 || exit 0"
  }]
}
\`\`\`

**7. Логирование всех действий**
\`\`\`json
{
  "matcher": ".*",
  "hooks": [{
    "type": "command",
    "command": "echo \"$(date): $TOOL_NAME\" >> ~/.claude/activity.log"
  }]
}
\`\`\`

**8. Автообновление CLAUDE.md**
\`\`\`json
{
  "event": "Stop",
  "hooks": [{
    "type": "command",
    "command": "git diff --name-only HEAD | grep -q 'package.json' && echo 'Напомни обновить CLAUDE.md' || true"
  }]
}
\`\`\`

### Переменные окружения в хуках

- \`$TOOL_NAME\` — имя инструмента (Edit, Bash, Read...)
- \`$TOOL_INPUT_PATH\` — путь файла (для Edit/Write)
- \`$TOOL_INPUT_CMD\` — команда (для Bash)

### Итог

- Хуки — shell-команды, реагирующие на события в Claude Code
- Настраиваются в settings.json в секции hooks
- Самые полезные: автолинт, форматирование, проверка типов
- Переменные среды дают доступ к параметрам вызова
`
  },
  {
    slug: 'starter-slash-commands',
    content: `## 10 slash-команд, чтобы использовать Claude Code как Boris Cherny

Эффективные slash-команды (skills) превращают Claude Code из инструмента в ассистента, который знает ваши процессы.

### 1. /ship — подготовка к деплою

\`\`\`markdown
---
name: ship
description: Подготовить текущую ветку к деплою
---

Подготовь ветку к деплою:
1. Запусти npm run lint и npm run type-check
2. Запусти npm test — покажи статус
3. Проверь git status — есть ли незакоммиченные изменения?
4. Покажи git log --oneline origin/main..HEAD
5. Дай рекомендацию: готово ли к merge?
\`\`\`

### 2. /debug — отладка ошибки

\`\`\`markdown
---
name: debug
description: Отладить ошибку или неожиданное поведение
---

Отладь следующую проблему: {{args}}

Шаги:
1. Найди релевантный код
2. Воспроизведи проблему (если возможно)
3. Найди root cause
4. Предложи минимальное исправление
5. Объясни почему это произошло
\`\`\`

### 3. /docs — генерация документации

\`\`\`markdown
---
name: docs
description: Создать документацию для функции или модуля
---

Создай документацию для: {{args}}

Формат: JSDoc комментарии для функций + README секция с примерами использования.
Только то, что неочевидно из кода.
\`\`\`

### 4. /migrate — создание миграции БД

\`\`\`markdown
---
name: migrate
description: Создать SQL миграцию
---

Создай SQL миграцию для: {{args}}

Требования:
- Файл: migrations/XXX_description.sql (следующий номер)
- Idempotent (IF NOT EXISTS / ON CONFLICT)
- Добавить в migrate.js если нужно
\`\`\`

### 5. /test — написание тестов

\`\`\`markdown
---
name: test
description: Написать тесты для функции или компонента
---

Напиши тесты для: {{args}}

Покрой:
- Happy path
- Edge cases (null, undefined, пустые массивы)
- Error cases
Используй фреймворк из package.json.
\`\`\`

### 6. /refactor — рефакторинг с объяснением

\`\`\`markdown
---
name: refactor
description: Рефакторинг с сохранением поведения
---

Отрефактори: {{args}}

Правила:
- Не меняй внешний API и поведение
- Сначала покажи план изменений
- Жди подтверждения перед изменением файлов
\`\`\`

### 7. /explain — объяснение кода

\`\`\`markdown
---
name: explain
description: Объяснить код на русском
---

Объясни на русском: {{args}}

Формат:
1. Что делает (одна строка)
2. Как работает (кратко)
3. Нетривиальные моменты
4. Где используется в проекте
\`\`\`

### 8. /pr — создание Pull Request

\`\`\`markdown
---
name: pr
description: Создать PR описание для текущей ветки
---

Создай описание для PR на основе git log и git diff main:
- Заголовок (до 70 символов)
- Что изменилось (буллеты)
- Как тестировать
- Скриншоты нужны? (да/нет)
\`\`\`

### 9. /todo — список задач из кода

\`\`\`markdown
---
name: todo
description: Найти все TODO и технический долг
---

Найди в проекте:
- Все TODO/FIXME/HACK комментарии
- Deprecated использования
- console.log оставленные в коде
- any в TypeScript

Сгруппируй по приоритету.
\`\`\`

### 10. /perf — анализ производительности

\`\`\`markdown
---
name: perf
description: Найти проблемы производительности
---

Проанализируй производительность: {{args}}

Ищи:
- N+1 запросы к БД
- Лишние ре-рендеры (React)
- Синхронный код блокирующий event loop
- Большие бандлы
Предлагай конкретные исправления.
\`\`\`

### Итог

- Skills сохраняют лучшие практики в командах
- Начните с /ship, /debug, /test — они окупятся сразу
- Адаптируйте промпты под стек вашего проекта
- Храните командные skills в .claude/skills/ в репозитории
`
  },
  {
    slug: 'starter-cowork-setup',
    content: `## Как настроить Claude Cowork

Claude Cowork — паттерн работы нескольких Claude Code агентов над одним проектом. Позволяет параллелизировать разработку.

### Концепция Cowork

В традиционной разработке несколько разработчиков работают над разными частями проекта одновременно. Cowork — то же самое, но с агентами Claude Code.

\`\`\`
Задача: Мигрировать проект на новую версию API

Агент 1: Обновляет endpoints в app/api/
Агент 2: Обновляет типы в types/
Агент 3: Обновляет тесты
Агент 4: Пишет документацию

→ Работают параллельно, каждый в своей git-ветке
→ PR-ы ревьюируются и мерджатся
\`\`\`

### Настройка через git worktrees

Git worktrees позволяют иметь несколько рабочих копий одного репозитория:

\`\`\`bash
# Создать worktree для каждого агента
git worktree add ../project-api-update feature/api-update
git worktree add ../project-types feature/types-update
git worktree add ../project-tests feature/tests-update

# Запустить агентов в каждой
cd ../project-api-update && claude "Обнови все endpoints..." &
cd ../project-types && claude "Обнови типы..." &
cd ../project-tests && claude "Обнови тесты..." &
\`\`\`

### Конфигурация для параллельной работы

\`\`\`json
// .claude/settings.json — настройки для командной работы
{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(npm test)",
      "Bash(npm run lint)"
    ],
    "deny": [
      "Bash(git push *)",
      "Bash(git merge *)",
      "Bash(git rebase *)"
    ]
  }
}
\`\`\`

Агенты коммитят, но не пушат и не мерджат — это остаётся под контролем человека.

### CLAUDE.md для Cowork

\`\`\`markdown
## Cowork правила

- Каждый агент работает в своей ветке
- Имена веток: feature/[task-description]
- Коммиты после каждой логической единицы работы
- Не трогать файлы вне своей области задачи

## Области ответственности
- app/api/ — API агент
- types/ — Types агент
- __tests__/ — Tests агент
\`\`\`

### Координация через задачи

Для сложных проектов — файл с разделением задач:

\`\`\`markdown
# tasks.md (не коммитить)

## Агент 1 — API
- [ ] app/api/users/route.ts
- [ ] app/api/posts/route.ts

## Агент 2 — Frontend
- [ ] components/UserList.tsx
- [ ] components/PostCard.tsx

## Агент 3 — Tests
- [ ] __tests__/api/users.test.ts
\`\`\`

### Итог

- Cowork = несколько агентов над одним проектом в параллельных ветках
- Git worktrees — правильный способ изолировать работу агентов
- Настройте разрешения: агенты коммитят, но не пушат
- CLAUDE.md описывает области ответственности каждого агента
`
  },
  {
    slug: 'current-limits-intro',
    content: `## Текущая граница возможностей Claude Code

Claude Code мощный, но не всесилен. Понимание ограничений помогает не разочаровываться и правильно строить воркфлоу.

### Технические ограничения

**Контекстное окно конечно.**
200K токенов звучит много, но большие кодовые базы (100+ файлов) быстро заполняют его. Симптомы: Claude «забывает» ранние части разговора, начинает повторяться.

**Нет постоянной памяти между сессиями (по умолчанию).**
Каждая новая сессия начинается с нуля. CLAUDE.md и MCP Memory server — способы обойти это, но не замена полноценной памяти.

**Не может видеть браузер в реальном времени.**
Claude Code не видит что происходит в открытом браузере. Нет скриншотов, нет отладки визуального рендеринга. Для frontend — нужно описывать проблему текстом или давать код для анализа.

### Когнитивные ограничения

**Галлюцинации API.**
Claude может уверенно написать код с несуществующими методами или параметрами. Особенно для свежих библиотек, которых нет в обучающих данных.

\`\`\`
// Claude может написать:
await prisma.user.findManyWithPagination({ ... })  // не существует

// Решение: просить проверять через документацию
> После написания кода — проверь что все методы Prisma действительно существуют
\`\`\`

**Не понимает визуальный дизайн.**
Описание «сделай красиво» — слишком абстрактно. Нужны конкретные требования: цвета, отступы, шрифты, референсы.

**Деградация на длинных сессиях.**
После нескольких часов работы в одной сессии качество обычно снижается. Признак — пора делать /clear или новую сессию.

### Ограничения скорости

- Rate limits на API (зависят от подписки)
- При тяжёлых задачах — задержки на несколько минут
- Параллельные агенты умножают потребление токенов

### Где Claude Code силён vs слаб

| Силён | Слаб |
|-------|------|
| Рефакторинг существующего кода | Визуальный дизайн |
| Написание тестов | Реальное дебаггинг в браузере |
| Понимание незнакомой кодовой базы | Свежие API (не в обучении) |
| SQL, shell, конфиги | Очень длинные контексты |
| Документация | Предсказание результата запуска |

### Как работать с ограничениями

1. **Короткие сессии** — начинайте заново при смене задачи
2. **CLAUDE.md** — фиксируйте важный контекст вне памяти сессии
3. **Верифицируйте API** — просите Claude проверять документацию
4. **Тесты как верификация** — запускайте тесты после каждого изменения

### Итог

- Контекст конечен, память между сессиями нет — используйте CLAUDE.md
- Галлюцинации API реальны — просите верифицировать через документацию
- Слаб в визуальном дизайне и дебаггинге в браузере
- Короткие сессии + коммиты = надёжный воркфлоу несмотря на ограничения
`
  },
  {
    slug: 'software-engineering-discipline-intro',
    content: `## Инженерия ПО как дисциплина

С Claude Code у вас появился очень быстрый исполнитель. Это усиливает значимость инженерной дисциплины: хороших решений — ещё больше хорошего, плохих решений — ещё больше плохого.

### Почему дисциплина важнее при использовании ИИ

Без ИИ: написать плохой код — медленно, накопить технический долг — заметно.
С ИИ: написать плохой код — мгновенно, накопить технический долг — незаметно, пока не стало огромной проблемой.

Claude Code ускоряет всё. В том числе ошибки архитектуры.

### Принципы, которые не отменяет ИИ

**1. Один коммит — одна логическая единица.**
Claude Code может сделать 50 изменений за один раз. Ваша задача — разбить это на атомарные коммиты с понятными сообщениями.

**2. Тесты — не опция.**
«Быстро написал, не тестировал» — у Claude Code нет интуиции «это может сломать X». Тесты — единственная надёжная верификация.

**3. Ревью изменений перед применением.**
Не принимайте изменения вслепую. Читайте diff. Claude Code может сделать правильную задачу не тем способом.

**4. Маленькие PR — лучше больших.**
ИИ провоцирует делать большие изменения за раз. Сопротивляйтесь. Маленький PR = понятный ревью = меньше багов в production.

### Инженерная культура с Claude Code

\`\`\`
Не: «Claude Code написал — всё готово»
Да: «Claude Code написал — я проверил, протестировал, закоммитил»

Не: «Это работает, не трогай»
Да: «Это работает. Есть тесты? Если нет — добавим»

Не: «Рефакторнем потом»
Да: «Рефакторнем сейчас, пока понимаем что делаем»
\`\`\`

### Автоматизация дисциплины

Используйте хуки для принудительного соблюдения правил:

\`\`\`json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{"type": "command", "command": "npm run lint 2>&1 | tail -5"}]
      }
    ]
  }
}
\`\`\`

Линтер запускается после каждого редактирования — дисциплина встроена в инструмент.

### Итог

- Claude Code ускоряет и хорошие, и плохие решения
- Инженерная дисциплина (тесты, маленькие PR, ревью) важнее, а не менее важна
- Автоматизируйте дисциплину через хуки и CI
- Вы — архитектор; Claude Code — исполнитель
`
  },
  {
    slug: 'structured-roadmap-intro',
    content: `## Структурированная дорожная карта

Системный подход к развитию навыков работы с Claude Code. От базового использования к профессиональному воркфлоу.

### Уровень 1: Базовый (1-2 недели)

**Цель:** освоить основной цикл работы.

Задачи:
- [ ] Установить Claude Code, пройти аутентификацию
- [ ] Попросить Claude Code объяснить существующий проект
- [ ] Добавить простую фичу под руководством Claude Code
- [ ] Создать CLAUDE.md для проекта
- [ ] Сделать коммит с помощью Claude Code

**Метрика:** уверенно использовать Claude Code для ежедневных задач.

### Уровень 2: Продвинутый (1-2 месяца)

**Цель:** настроить персональный воркфлоу.

Задачи:
- [ ] Создать 5 собственных skills
- [ ] Настроить хуки (автолинт + уведомления)
- [ ] Подключить 2-3 MCP-сервера
- [ ] Использовать Plan Mode для крупных задач
- [ ] Оптимизировать управление контекстом

**Метрика:** Claude Code экономит 2+ часа в день.

### Уровень 3: Экспертный (3-6 месяцев)

**Цель:** автономные воркфлоу и командное применение.

Задачи:
- [ ] Создать собственный MCP-сервер
- [ ] Настроить Cowork для параллельной работы
- [ ] Внедрить Claude Code в CI/CD
- [ ] Написать командный CLAUDE.md
- [ ] Обучить команду

**Метрика:** команда вдвое быстрее выпускает фичи.

### Рекомендуемый путь обучения

\`\`\`
Неделя 1: Установка + первые задачи
Неделя 2: CLAUDE.md + skills
Неделя 3: Хуки + настройка разрешений
Неделя 4: MCP + первый сервер
Месяц 2: Автономные воркфлоу
Месяц 3+: Командное применение
\`\`\`

### Ресурсы для продолжения

- Официальная документация: code.claude.com/docs
- Репозиторий MCP-серверов: github.com/modelcontextprotocol/servers
- Сообщество: discord.gg/anthropic

### Практические проекты для закрепления

1. **Мигрировать старый проект** — попросить Claude Code разобраться в существующей кодовой базе и провести рефакторинг
2. **Создать свой MCP-сервер** — для внутреннего инструмента компании
3. **Автоматизировать code review** — настроить skill + hook для автоматической проверки

### Итог

- Освоение Claude Code — это итеративный процесс, не разовое обучение
- Начните с базового использования, добавляйте сложность постепенно
- Лучший учитель — реальные задачи вашего проекта
- Делитесь навыками с командой: skills и CLAUDE.md в репозитории
`
  }
]

async function main() {
  console.log(`Seeding content for ${lessons.length} lessons...`)

  for (const lesson of lessons) {
    const { rowCount } = await pool.query(
      'UPDATE lessons SET content = $2 WHERE slug = $1',
      [lesson.slug, lesson.content]
    )

    if (rowCount === 0) {
      console.warn(`  ⚠ No lesson found with slug: ${lesson.slug}`)
    } else {
      console.log(`  ✓ ${lesson.slug}`)
    }
  }

  console.log('Done.')
  await pool.end()
}

main().catch((err) => {
  console.error(err)
  process.exit(1)
})
